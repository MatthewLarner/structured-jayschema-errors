module.exports = {
    additionalProperties: require('./additionalProperties'),
    enum: require('./enum'),
    formatErrors: require('./formatErrors'),
    nested: require('./nested'),
    requiredProperties: require('./requiredProperties'),
    types: require('./types')
};